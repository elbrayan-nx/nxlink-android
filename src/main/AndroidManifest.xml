<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nxlink">

    <!-- ── Permissions ──────────────────────────────────────────────────── -->

    <!-- TCP socket to the Switch -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Required to read files from shared storage on older APIs -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
        On Android 13+ (API 33+) we use the system file picker via
        ActivityResultContracts.GetContent(), which grants a scoped URI
        permission automatically — no READ_MEDIA_* permission needed for
        arbitrary file types (.nro is not a media type).
    -->

    <!-- ── Application ──────────────────────────────────────────────────── -->
    <application
        android:allowBackup="true"
        android:label="nxlink"
        android:icon="@mipmap/nxlink"
        android:roundIcon="@mipmap/nxlink_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NXLink"
        android:usesCleartextTraffic="true">
        <!--
            usesCleartextTraffic="true" is required because nxlink sends raw
            unencrypted TCP to the Switch's local IP.  This is only used on
            your own LAN and no sensitive data is transmitted beyond the NRO
            payload itself.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Optional: let the app be the default handler when a user taps
                an .nro file from a file manager or browser download.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="*/*" />
            </intent-filter>

        </activity>
    </application>

</manifest>
